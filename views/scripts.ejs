<%- include("partials/header.ejs") %>
<div class="container">
    <div class="bloglist">
    <% for(let i=0;i<title.length;i++){ %>
        <form action="/expand" method="post" onclick="submit()">
            <div class="blog" id="<%= i%>">
                <h2><input type="number" value="<%= i%>" class="blognum" name="blogex" readonly="true"/><%=(i+1)+". "+title[i] %></h2>
            </div> 
        </form>
    <% } %>
    </div>
</div>

<%- include("partials/footer.ejs") %>